[project]
name = "insis-picker"
version = "0.1.0"
description = "Watch VŠE InSIS for a free timetable slot and claim it."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "requests>=2.34.2",
]
